<div id="burger-body">
<h1>Free Burgers</h1>
<img alt="image of a burger" id="burger-img" src="/assets/img/burger_25327-97.jpg"/>


<h2>Burger that are not yet eaten!</h2>




<ul>
    {{#each burgers}}
    {{#unless devoured}}
    <li>{{burger}}
    {{/unless}}
    {{/each}}
</ul>

<h2>Burgers that are eaten!</h2>

<ul>
    {{#each burgers}}
    {{#if devoured}}
    <li>{{burger}}</li>
    {{/if}}
    {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

    <div class="form-group">
        <label for="burger">Burger Name:</label>
        <input type="text" id="burger" name="name">
    </div>

    <div class="form-group">
        <label for="burger-devoured">Eaten</label><br>
        <input type="radio" name="burger-devoured" value="1" checked>Devoured!<br>
        <input type="radio" name="burger-devoured" value="0"> Not devoured
    </div>

    <button type="submit">Add Burger</button>
</form>


</div>
